# Tasks for Cursor AI (Consolidated)

> This file consolidates tasks from the checklist, combines duplicates, and preserves completed items.

- **Source file:** Very Final Fixes.xlsx - Checklist.csv
- **Legend:** ✅ = done, ⏳ = pending


---

### 1. Deactivate suppliers and categories only if they don’t have any products

- **Status:** ⏳ Pending
- **Severity:** high

**Context / Combined descriptions:**

- Deactivate suppliers and categories only if they don’t have any products. if they do, require deactivation of their products first (deactivate only empty categories).

**Cursor Prompt:**

```
Context:
Deactivate suppliers and categories only if they don’t have any products. if they do, require deactivation of their products first (deactivate only empty categories).

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 2. Add filter for Suppliers (all, active, inactive) and fix it in products, Customers, Categories,Suppliers to be for all n

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- add filter for Suppliers (all, active, inactive) and fix it in products, Customers, Categories,Suppliers to be for all not for exsting page

**Cursor Prompt:**

```
Context:
add filter for Suppliers (all, active, inactive) and fix it in products, Customers, Categories,Suppliers to be for all not for exsting page

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 3. Fix pganation and sorting arrows

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- fix pganation and sorting arrows

**Cursor Prompt:**

```
Context:
fix pganation and sorting arrows

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 4. Fix the price to be after vat

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- fix the price to be after vat

**Cursor Prompt:**

```
Context:
fix the price to be after vat

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 5. In code fornend/index

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- in code fornend/index.js fix the currency for paypal to be current currncy

**Cursor Prompt:**

```
Context:
in code fornend/index.js fix the currency for paypal to be current currncy

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 6. In code fornend/pages/Home

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- in code fornend/pages/Home.jsx the categoreis not hard coded

**Cursor Prompt:**

```
Context:
in code fornend/pages/Home.jsx the categoreis not hard coded

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 7. In code frontend/context CartContext there is more than one

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- in code frontend/context CartContext there is more than one

**Cursor Prompt:**

```
Context:
in code frontend/context CartContext there is more than one

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 8. In Suppliers, Categories, Customers, and in products page to be deafult first sorting for id (big to small) new is first

- **Status:** ⏳ Pending
- **Severity:** medium

**Context / Combined descriptions:**

- in Suppliers, Categories, Customers, and in products page to be deafult first sorting for id (big to small) new is first

**Cursor Prompt:**

```
Context:
in Suppliers, Categories, Customers, and in products page to be deafult first sorting for id (big to small) new is first

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 9. Refresh button in cart not working

- **Status:** ⏳ Pending
- **Severity:** medium

- **Notes:** what?

**Context / Combined descriptions:**

- refresh button in cart not working

**Cursor Prompt:**

```
Context:
refresh button in cart not working

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 10. 1- when go to product page it shows first the page of not fount filters and then the product, fix it to show the product

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 1- when go to product page it shows first the page of not fount filters and then the product, fix it to show the products instantly

**Cursor Prompt:**

```
Context:
1- when go to product page it shows first the page of not fount filters and then the product, fix it to show the products instantly

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 11. 1-الexpress session لازم تحفظ products الي موجودين بcart لكل user ومش من database

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 1-الexpress session لازم تحفظ products الي موجودين بcart لكل user ومش من database

**Cursor Prompt:**

```
Context:
1-الexpress session لازم تحفظ products الي موجودين بcart لكل user ومش من database

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 12. 2- in the product page the slider of the min and max price changes according to the current filter

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 2- in the product page the slider of the min and max price changes according to the current filter.

**Cursor Prompt:**

```
Context:
2- in the product page the slider of the min and max price changes according to the current filter.

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 13. 2-بال settings العملة لازم تتغير قيمتها بكل الموقع من api اشي כללי ويلا يعمل مشاكل مع vat

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 2-بال settings العملة لازم تتغير قيمتها بكل الموقع من api اشي כללי ويلا يعمل مشاكل مع vat

**Cursor Prompt:**

```
Context:
2-بال settings العملة لازم تتغير قيمتها بكل الموقع من api اشي כללי ويلا يعمل مشاكل مع vat

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 14. 3- in manger in each page that contains statistics numbers (the boxes) it must change according to the filters

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 3- in manger in each page that contains statistics numbers (the boxes) it must change according to the filters

**Cursor Prompt:**

```
Context:
3- in manger in each page that contains statistics numbers (the boxes) it must change according to the filters

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 15. 3- كيف التخفيض بشتغل اذا معمول عل فئة معينة وبالorder في products تانية مش داخلة بالتخفيض

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 3- كيف التخفيض بشتغل اذا معمول عل فئة معينة وبالorder في products تانية مش داخلة بالتخفيض

**Cursor Prompt:**

```
Context:
3- كيف التخفيض بشتغل اذا معمول عل فئة معينة وبالorder في products تانية مش داخلة بالتخفيض

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 16. 4- بصفحة الproducts لslider مجال min والmax لازم يتبدل لما نغير category او manufacturer او تنين مع بغض يعني يعرض المجال

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 4- بصفحة الproducts لslider مجال min والmax لازم يتبدل لما نغير category او manufacturer او تنين مع بغض يعني يعرض المجال الحالي تبع الفلاتر

**Cursor Prompt:**

```
Context:
4- بصفحة الproducts لslider مجال min والmax لازم يتبدل لما نغير category او manufacturer او تنين مع بغض يعني يعرض المجال الحالي تبع الفلاتر

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 17. 4-add pganation in order-history page

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 4-add pganation in order-history page

**Cursor Prompt:**

```
Context:
4-add pganation in order-history page

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 18. 5-When a supplier or category is inactive, it should still appear in the filters, but in the product edit or add popup,

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 5-When a supplier or category is inactive, it should still appear in the filters, but in the product edit or add popup, they should be displayed (visible) but not selectable.

**Cursor Prompt:**

```
Context:
5-When a supplier or category is inactive, it should still appear in the filters, but in the product edit or add popup, they should be displayed (visible) but not selectable.

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 19. 6-in dashboard the bar graph to be sorted according to count per units of best sellers

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 6-in dashboard the bar graph to be sorted according to count per units of best sellers

**Cursor Prompt:**

```
Context:
6-in dashboard the bar graph to be sorted according to count per units of best sellers

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 20. 7-remove the credit cart option from checkou

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- 7-remove the credit cart option from checkou

**Cursor Prompt:**

```
Context:
7-remove the credit cart option from checkou

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 21. ADD IN THE INVOICE THE VAT RATE

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- ADD IN THE INVOICE THE VAT RATE

**Cursor Prompt:**

```
Context:
ADD IN THE INVOICE THE VAT RATE

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 22. Filters to be for all the list not for the exsiting page

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- filters to be for all the list not for the exsiting page

**Cursor Prompt:**

```
Context:
filters to be for all the list not for the exsiting page

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 23. Fix promotion not working when applies in checkout

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- fix promotion not working when applies in checkout

**Cursor Prompt:**

```
Context:
fix promotion not working when applies in checkout

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 24. For admin account delete all the header (home, products, cart, contact, about, and search) and search bar only to be pro

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- for admin account delete all the header (home, products, cart, contact, about, and search) and search bar only to be profile without order history

**Cursor Prompt:**

```
Context:
for admin account delete all the header (home, products, cart, contact, about, and search) and search bar only to be profile without order history

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 25. In add/edit Supplier add validation for the phone to be same validation from sign in

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in add/edit Supplier add validation for the phone to be same validation from sign in

**Cursor Prompt:**

```
Context:
in add/edit Supplier add validation for the phone to be same validation from sign in

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 26. In customer to delete customer only if all order for him in statsus Deliverd

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in customer to delete customer only if all order for him in statsus Deliverd

**Cursor Prompt:**

```
Context:
in customer to delete customer only if all order for him in statsus Deliverd

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 27. In home when click on Promotion Details popup fix Applies To work for the product or categories theat set from manger

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in home when click on Promotion Details popup fix Applies To work for the product or categories theat set from manger

**Cursor Prompt:**

```
Context:
in home when click on Promotion Details popup fix Applies To work for the product or categories theat set from manger

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 28. In product details page remove the favoriete button(heart icon)

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in product details page remove the favoriete button(heart icon)

**Cursor Prompt:**

```
Context:
in product details page remove the favoriete button(heart icon)

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 29. In product page when stock is zero to display Stock: 0

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in product page when stock is zero to display Stock: 0

**Cursor Prompt:**

```
Context:
in product page when stock is zero to display Stock: 0

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 30. In the invoice in the email fix the currncy symbol to be showed correctly

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in the invoice in the email fix the currncy symbol to be showed correctly

**Cursor Prompt:**

```
Context:
in the invoice in the email fix the currncy symbol to be showed correctly

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 31. In the promotion bar in home without to …

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- in the promotion bar in home without to …

**Cursor Prompt:**

```
Context:
in the promotion bar in home without to …

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 32. On mange orders add one more square for staus Shipped and add filtter for status and filter for email

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- on mange orders add one more square for staus Shipped and add filtter for status and filter for email

**Cursor Prompt:**

```
Context:
on mange orders add one more square for staus Shipped and add filtter for status and filter for email

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 33. Sesson storage from one user to another keeps the same items though its not the same user

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- sesson storage from one user to another keeps the same items though its not the same user

**Cursor Prompt:**

```
Context:
sesson storage from one user to another keeps the same items though its not the same user

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 34. Suppliers - check if pagination exists and if

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- suppliers - check if pagination exists and if

**Cursor Prompt:**

```
Context:
suppliers - check if pagination exists and if

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 35. When making order the first status for the customers in order-history to be pending not confirmed

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- when making order the first status for the customers in order-history to be pending not confirmed

**Cursor Prompt:**

```
Context:
when making order the first status for the customers in order-history to be pending not confirmed

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 36. When the cart is empty the box that exist move it to the center

- **Status:** ⏳ Pending
- **Severity:** unspecified

**Context / Combined descriptions:**

- when the cart is empty the box that exist move it to the center

**Cursor Prompt:**

```
Context:
when the cart is empty the box that exist move it to the center

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 37. In dashboard 'units left' to be only in red and make the card for each product little bigger to 'units left' fit in one

- **Status:** ✅ Done
- **Severity:** high

- **Notes:** ?

**Context / Combined descriptions:**

- in dashboard 'units left' to be only in red and make the card for each product little bigger to 'units left' fit in one line

**Cursor Prompt:**

```
Context:
in dashboard 'units left' to be only in red and make the card for each product little bigger to 'units left' fit in one line

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 38. Fix Inputs in login in page and sign in page and in profile page and Suppliers popup and Categories popup

- **Status:** ✅ Done
- **Severity:** medium

**Context / Combined descriptions:**

- fix Inputs in login in page and sign in page and in profile page and Suppliers popup and Categories popup

**Cursor Prompt:**

```
Context:
fix Inputs in login in page and sign in page and in profile page and Suppliers popup and Categories popup

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 39. In Categories and Suppliers when add or edit to update the list without refreshing the page and fix also the search with

- **Status:** ✅ Done
- **Severity:** unspecified

**Context / Combined descriptions:**

- in Categories and Suppliers when add or edit to update the list without refreshing the page and fix also the search without refresh and when deactived to change the status and the button to restorre, make space between buttone in one line

**Cursor Prompt:**

```
Context:
in Categories and Suppliers when add or edit to update the list without refreshing the page and fix also the search without refresh and when deactived to change the status and the button to restorre, make space between buttone in one line

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```

---

### 40. In dashboard line graph order count to be without the currency and in bar graph to be sorted from big to small according

- **Status:** ✅ Done
- **Severity:** unspecified

**Context / Combined descriptions:**

- in dashboard line graph order count to be without the currency and in bar graph to be sorted from big to small according to total sales

**Cursor Prompt:**

```
Context:
in dashboard line graph order count to be without the currency and in bar graph to be sorted from big to small according to total sales

Requirements:
- Implement the fix/feature described above.
- Keep the diff minimal and scoped to the relevant component(s) and API(s).
- Preserve existing pagination, filters, and sorting behavior where applicable.
- Add basic error handling and a success toast where it improves UX.

Acceptance:
- The described behavior works as expected.
- No regressions on related pages.
- Passes ESLint/TypeScript (if enabled).

Now locate the relevant files in the repo and show the planned diffs, then implement.
```